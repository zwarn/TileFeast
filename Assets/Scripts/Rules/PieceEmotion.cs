namespace Rules
{
    // Ordered so that cast-to-int gives "most negative wins": Happy=0, Neutral=1, Sad=2
    public enum PieceEmotion
    {
        Happy = 0,
        Neutral = 1,
        Sad = 2
    }
}
